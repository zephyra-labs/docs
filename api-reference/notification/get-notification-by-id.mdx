---
title: "Get Notification by ID"
openapi: GET `/api/notification/{id}`
description: "Retrieve a single notification by its ID."
---

# GET `/api/notification/{id}`

Get a notification by its ID.

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `id` | `string` | yes | Notification ID (path parameter) |

## Responses

### 200 — OK

Returns the requested notification.

```json
{
  "id": "notif-uuid-123",
  "userId": "user-uuid-456",
  "executorId": "user-uuid-789",
  "type": "system",
  "title": "New Trade Completed",
  "message": "Your trade #123 has been completed successfully.",
  "read": false,
  "createdAt": 1699286400000,
  "updatedAt": 1699372800000,
  "extraData": {
    "tradeId": "trade-uuid-987"
  }
}
```

### 400 — Bad Request

Returned if the `id` parameter is missing.

```json
{
  "success": false,
  "message": "Missing id parameter"
}
```

### 401 — Unauthorized

Returned if the user is not authenticated.

```json
{
  "success": false,
  "message": "Missing or invalid Authorization header"
}
```

### 404 — Not Found

Returned if the notification with the specified ID does not exist.

```json
{
  "success": false,
  "message": "Notification not found"
}
```

### 500 — Server Error

Returned if there is a server error while fetching the notification.

```json
{
  "success": false,
  "message": "Failed to fetch notification"
}
```

## Authentication

This endpoint requires a **Bearer Token**.

## Example Request

```bash
curl -X GET "https://api.zephyra.dev/api/notification/notif-uuid-123" \
  -H "Authorization: Bearer <token>" \
  -H "Accept: application/json"
```

## Example Response

```json
{
  "id": "notif-uuid-123",
  "userId": "user-uuid-456",
  "executorId": "user-uuid-789",
  "type": "system",
  "title": "New Trade Completed",
  "message": "Your trade #123 has been completed successfully.",
  "read": false,
  "createdAt": 1699286400000,
  "updatedAt": 1699372800000,
  "extraData": {
    "tradeId": "trade-uuid-987"
  }
}
```
