---
title: "Record Wallet Disconnect"
openapi: POST `/api/wallet/log-disconnect`
description: "Create a log entry for a wallet disconnect event for a user."
---

# POST `/api/wallet/log-disconnect`

Record a wallet disconnect event for a specific user account.

## Request Body

| Name | Type | Required | Description |
|------|------|-----------|-------------|
| `account` | `string` | yes | Wallet address of the user |
| `action` | `string` | yes | Action performed (`connect`, `disconnect`, `switchNetwork`, `signMessage`) |
| `timestamp` | `number` | yes | Unix timestamp of the disconnect event |
| `meta` | `object` | no | Optional metadata (IP, device info, session ID) |

> Content-Type: `application/json`

### Example Request Body

```json
{
  "account": "0x123abc...",
  "action": "disconnect",
  "timestamp": 1699286400000,
  "meta": {
    "ip": "192.168.1.1",
    "device": "Chrome on Windows"
  }
}
```

## Responses

### 201 — Created

Wallet disconnect log successfully created.

```json
{
  "success": true,
  "data": {
    "account": "0x123abc...",
    "action": "disconnect",
    "timestamp": 1699286400000,
    "meta": {
      "ip": "192.168.1.1",
      "device": "Chrome on Windows"
    }
  }
}
```

### 400 — Bad Request

Returned when the request body contains invalid or missing fields.

```json
{
  "success": false,
  "message": "Account parameter is required"
}
```

### 401 — Unauthorized

Returned if the request is missing or contains an invalid authentication token.

```json
{
  "success": false,
  "message": "Missing or invalid Authorization header"
}
```

### 500 — Server Error

```json
{
  "success": false,
  "message": "Failed to log wallet disconnect"
}
```

## Authentication

This endpoint requires a **Bearer Token** for authentication.

## Example Request

```bash
curl -X POST "https://api.zephyra.dev/api/wallet/log-disconnect" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "account": "0x123abc...",
    "action": "disconnect",
    "timestamp": 1699286400000,
    "meta": {
      "ip": "192.168.1.1",
      "device": "Chrome on Windows"
    }
  }'
```

## Example Response

```json
{
  "success": true,
  "data": {
    "account": "0x123abc...",
    "action": "disconnect",
    "timestamp": 1699286400000,
    "meta": {
      "ip": "192.168.1.1",
      "device": "Chrome on Windows"
    }
  }
}
```